import '../styles/globals.css'
import { Se} from "next-auth/client"

function MyApp({ Component, pageProps }) {
  return (
    <SessionProvide?r
    <Component {...pageProps} />
    )
}

export default MyApp
